import { Options } from './interfaces/options';
/**
 * Used to set default options for indexing data.
 *
 * @export
 * @param {Options} userOptions
 * @return {Options}
 */
export declare function setOptions(userOptions: Options): Options;
/**
 * Return the current options.
 *
 * @export
 * @return {Options}
 */
export declare function getOptions(): Options;
